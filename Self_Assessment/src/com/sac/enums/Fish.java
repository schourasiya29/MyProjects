package com.sac.enums;

public enum Fish {
	Star, Gold, Guppy;
	
	/* We can write main method inside an enum and run it directly as a class */
	public static void main(String[] args) {
		System.out.println("ENUM Main method called");
	}
}
